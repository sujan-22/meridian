# Test database. Reset and reseeded by `pnpm test:setup`; never points at the
# database that holds real tracked time.
DATABASE_URL=postgresql://timeheist:timeheist@localhost:5432/quanta_test
DATABASE_URL_UNPOOLED=postgresql://timeheist:timeheist@localhost:5432/quanta_test
NEXT_PUBLIC_APP_URL=http://localhost:3100
